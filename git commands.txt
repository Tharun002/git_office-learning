git branch -M main -- current branch to main

git status -- which branch, what changes made


git init

git status

git add .(file name)
git commit -m "message for commiting"
git push origin main

I have updated till this in local to remote and now i am adding one more line.

git branch

git checkout -b branch1 

git push origin branch1


i am just checking 

git git 